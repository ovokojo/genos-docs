import 'package:flutter/material.dart';

class AppColors {
  // Light theme colors
  static const Color lightPrimary = Colors.blue;
  static const Color lightSecondary = Colors.blue;
  static const Color lightBackground = Colors.white;
  static const Color lightText = Colors.black;
  static const Color lightTextLight = Colors.white;

  // Dark theme colors
  static const Color darkPrimary = Colors.redAccent;
  static const Color darkSecondary = Colors.blueAccent;
  static const Color darkBackground = Color(0xFF121212);
  static const Color darkText = Colors.white;

  // Common colors
  static const Color error = Colors.red;
  static const Color success = Colors.green;
}
